<h2><%= @title %></h2>

<%= simple_form_for @person, :url => admin_people_path do |f| %>

  <%= render :partial => 'admin/people/form', :locals => { :f => f } %>
	
  <%= render :partial => 'admin/shared/form_buttons', :locals => { f: f, item: @person } %>

<% end %>
