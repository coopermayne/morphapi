<div class="row">
  <div class='col-md-6'>
    <h3>project page image</h3>
    <% if f.object.primary_image %>
      <a class="btn" rel="popover" data-img=<%= f.object.primary_image.name.mobile.url %>>
        <%= image_tag f.object.primary_image.name.mobile, class: "img-responsive thumby" %>
      </a>
    <% else %>
      none
    <% end %>
  </div>
  <div class='col-md-6'>
    <h3>index page image</h3>
    <% if f.object.index_image %>
      <a class="btn" rel="popover" data-img=<%= f.object.primary_image.name.mobile.url %>>
        <%= image_tag f.object.index_image.name.mobile, class: "img-responsive thumby" %>
      </a>
    <% else %>
      none
    <% end %>
  </div>
  **change with checkboxes below
</div>
