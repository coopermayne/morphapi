<div class="row">
	<div class="col-sm-6">
		<h2><%= @title %></h2>
	</div>

	<div class="col-sm-6 right">
		<%= link_to fa_icon("plus", text: "new item").html_safe, new_admin_bibliography_item_path, class: "btn btn-primary" %><br>
	</div>
</div>

<%= render :partial => 'admin/bibliography_items/table', :locals => { :bibliography_items => @bibliography_items } %>
