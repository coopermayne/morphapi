<fieldset class="well">
  <%= f.hidden_field :_destroy %>
  <%= link_to "#{fa_icon "trash"}remove".html_safe, "#", class: "remove_fields" %>

  <%= f.input :title %> 
  <%= f.input :in_gallery %>

  <%= f.input :file_type_id, collection: FileType.all, prompt: "Type..." %><br />

  <%= f.input :name %> 
</fieldset>
