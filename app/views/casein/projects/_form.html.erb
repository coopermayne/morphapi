<!-- Scaffolding generated by Casein v5.1.1.5 -->


<h2>type/subtyle</h2>
<%= hidden_field_tag "project[project_type_ids][]", nil %>

<% ProjectType.roots.each do |project_type| %>
  <%= check_box_tag "project[project_type_ids][]", project_type.id, f.object.project_type_ids.include?(project_type.id), id: dom_id(project_type) %>
  <%= label_tag dom_id(project_type), project_type.title %><br>

  <% project_type.children.each do |child| %>
    --<%= check_box_tag "project[project_type_ids][]", child.id, f.object.project_type_ids.include?(child.id), id: dom_id(child) %>
    <%= label_tag dom_id(child), child.title %><br>


    <% child.children.each do |ch| %>
      ----<%= check_box_tag "project[project_type_ids][]", ch.id, f.object.project_type_ids.include?(ch.id), id: dom_id(ch) %>
      <%= label_tag dom_id(ch), ch.title %><br>
    <% end %>

  <% end %>

<% end %>

<h2>images</h2>

<div class="row">

  <% f.object.uploads.group_by{|upload| upload.file_type}.each do |type, upload_group| %>
    <h1><%= type.title %></h1>
    <%= f.fields_for :uploads, upload_group.sort_by{|i| i.rank} do |builder| %>
      <div class="row">
        <div class="col-sm-2">
          <%= image_tag builder.object.name, class: "img-responsive" %>
        </div>
        <div class="col-sm-10">
          <%= builder.number_field :rank, size: 3 %>
          <br \>
          <%= builder.collection_select:file_type_id, FileType.all, :id, :title, prompt: "Select a type..." %>
          <br \>
          show? <%= builder.check_box :in_gallery %>
          <br \>
          delete? <%= builder.check_box :_destroy %>
      </div>
    </div>
  <% end %>
<% end %>

</div>

<h2>sorting</h2>
<div class="row">
	<div class="col-lg-6">
  <%= f.label :seciton %>
  <%= casein_collection_select f, f.object, :project, :section_id, Section.all, :id, :title %>
  </div>
</div>

<h2>Roles</h2>
<div class="row">
	<div class="col-lg-6">
  <%= f.fields_for :roles do |builder| %>
      <%= render "form_for_roles", f:builder %>
  <% end %>
  </div>
</div>

<%= link_to_add_fields "Add Role", f, :roles %>

<h2>Other Stuff</h2>
<div class="row">
	<div class="col-lg-6">
		<%= casein_text_field f, f.object, :title %>
	</div>

	<div class="col-lg-6">
    <%= image_tag f.object.primary_image.name_url, class: "img-responsive" %>
	</div>

</div>

<div class="row">
  <div class="col-lg-12">
    <%= casein_text_area f, f.object, :overview, rows: "8" %>
  </div>
</div>
<div class="row">
  <div class="col-lg-12">
    <%= casein_text_area f, f.object, :description, rows: "20" %>
  </div>
  <div class="col-lg-12">
    <%= casein_text_area f, f.object, :program %>
  </div>
</div>

<h2>details</h2>

<div class="row">
  <div class="col-lg-6">
    <%= casein_text_field f, f.object, :client %>
  </div>
  <div class="col-lg-6">
    <%= casein_text_field f, f.object, :size %>
  </div>

  <div class="col-lg-6">
    <%= casein_text_field f, f.object, :height %>
  </div>

  <div class="col-lg-6">
    <%= casein_text_field f, f.object, :site_area %>
  </div>
  <div class="col-lg-6">
    <%= casein_text_field f, f.object, :lat %>
  </div>
  <div class="col-lg-6">
    <%= casein_text_field f, f.object, :lon %>
  </div>
  <div class="col-lg-6">
    <%= casein_text_field f, f.object, :street_address %>
  </div>
  <div class="col-lg-6">
    <%= casein_text_field f, f.object, :zip %>
  </div>
</div>
<h2>dates</h2>
<div class="row">
  <div class="col-lg-6">
    <%= casein_date_select f, f.object, :design_sdate %>
  </div>
  <div class="col-lg-6">
    <%= casein_date_select f, f.object, :design_sdate %>
  </div>
  <div class="col-lg-6">
    <%= casein_date_select f, f.object, :constr_sdate %>
  </div>
  <div class="col-lg-6">
    <%= casein_date_select f, f.object, :constr_edate %>
  </div>
  <div class="col-lg-6">
    <%= casein_date_select f, f.object, :open_date %>
  </div>
  <div class="col-lg-6">
    <%= casein_date_select f, f.object, :close_date %>
  </div>
</div>

